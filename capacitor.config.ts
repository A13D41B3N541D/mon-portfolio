import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MonPortfolio',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
